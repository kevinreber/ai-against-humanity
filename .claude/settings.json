{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$TOOL_INPUT\" | grep -q 'git commit'; then VITE_CONVEX_URL=${VITE_CONVEX_URL:-https://placeholder.convex.cloud} PLAYWRIGHT=true npx playwright test --project=chromium; fi"
          }
        ]
      }
    ]
  }
}
