// Shared constants for game limits — kept in a separate file to avoid
// pulling Node.js dependencies (OpenAI, crypto) into query/mutation bundles.

export const MAX_AI_PLAYERS_PER_GAME = 3;
export const MAX_AI_PLAYERS_WITH_OWN_KEY = 5;
