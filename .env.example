# Convex
VITE_CONVEX_URL=https://your-project.convex.cloud

# Convex Dashboard Environment Variables (set in Convex Dashboard, not here)
# OPENAI_API_KEY=sk-...
# UPSTASH_REDIS_URL=https://...     (optional - enables rate limiting)
# UPSTASH_REDIS_TOKEN=...           (optional - enables rate limiting)
# ENCRYPTION_KEY=<64-char hex string>  (required for user API key storage - generate with: openssl rand -hex 32)
